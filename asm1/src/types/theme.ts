export interface ThemeColors {
  background: string;
  text: string;
  primary: string;
  secondary: string;
  success: string;
  danger: string;
}

export interface Theme {
  light: ThemeColors;
  dark: ThemeColors;
}
